services:
  devcontainer:
    command: sh -c 'while sleep 1; do :; done'
    image: ghcr.io/acilearning/docker-haskell:9.4.1
    volumes:
      - .:/workspaces/monadoc
    working_dir: /workspaces/monadoc
